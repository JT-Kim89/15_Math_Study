# 선형대수학 I 이론 보강 노트

- 학년: 1학년
- 영역: 선형대수 기초

이 문서는 과목별로 챕터 이론을 한 번 더 길게 정리하기 위한 보조 노트입니다. 각 챕터의 `notebook.ipynb`에도 같은 이론이 실행 흐름에 맞추어 들어 있습니다.

## 과목 전체를 보는 관점
- 정의를 먼저 고정하고, 예제는 정의가 실제로 어떻게 작동하는지 확인하는 도구로 사용한다.
- 정리는 계산을 빠르게 하는 공식이 아니라, 어떤 가정 아래에서 계산이 정당한지를 말해준다.
- 반례는 이론의 경계를 확인하는 가장 빠른 방법이다.
- 코딩 실습은 정리의 증명이 아니라 직관, 수치 검산, 반례 탐색을 돕는 실험이다.

## 01. 벡터와 선형결합

### 핵심 개념
- 벡터
- 행렬
- 선형변환
- 기저

### 이론 골격
- 벡터공간은 덧셈과 스칼라배가 닫혀 있는 추상적 좌표 공간이다.
- 기저는 모든 벡터를 유일하게 표현하게 하는 최소 생성 집합이다.
- 행렬은 선형변환의 좌표 표현이고, 행렬곱은 변환의 합성이다.
- 고유값과 고유벡터는 변환이 방향을 보존하면서 크기만 바꾸는 특별한 축을 나타낸다.

### 반드시 이해할 정리/원리
- 랭크-널리티 정리는 해공간의 자유도와 변환 이미지의 차원을 보존 관계로 묶는다.
- 스펙트럼 정리는 대칭행렬을 직교기저에서 대각화해 해석을 단순화한다.
- 최소제곱은 관측 벡터를 열공간에 정사영하는 문제로 해석된다.
- SVD는 임의 행렬을 회전, 축척, 회전의 조합으로 분해해 저차원 근사를 가능하게 한다.

### 공부할 때의 기준 질문
- 이 챕터의 기본 예제: 두 벡터의 선형결합이 평면의 격자를 어떻게 채우는지 본다.
- 이 챕터의 응용 예제: 두 재료 배합 비율로 가능한 색상/성분 조합을 시각화한다.
- 어떤 정의가 계산을 가능하게 만들고, 어떤 정리가 계산을 정당화하는가?
- 반례를 만들려면 어떤 가정을 먼저 제거해 볼 수 있는가?

## 02. 행렬곱과 선형변환

### 핵심 개념
- 벡터
- 행렬
- 선형변환
- 기저

### 이론 골격
- 벡터공간은 덧셈과 스칼라배가 닫혀 있는 추상적 좌표 공간이다.
- 기저는 모든 벡터를 유일하게 표현하게 하는 최소 생성 집합이다.
- 행렬은 선형변환의 좌표 표현이고, 행렬곱은 변환의 합성이다.
- 고유값과 고유벡터는 변환이 방향을 보존하면서 크기만 바꾸는 특별한 축을 나타낸다.

### 반드시 이해할 정리/원리
- 랭크-널리티 정리는 해공간의 자유도와 변환 이미지의 차원을 보존 관계로 묶는다.
- 스펙트럼 정리는 대칭행렬을 직교기저에서 대각화해 해석을 단순화한다.
- 최소제곱은 관측 벡터를 열공간에 정사영하는 문제로 해석된다.
- SVD는 임의 행렬을 회전, 축척, 회전의 조합으로 분해해 저차원 근사를 가능하게 한다.

### 공부할 때의 기준 질문
- 이 챕터의 기본 예제: 행렬이 단위정사각형을 평행사변형으로 보내는 과정을 그린다.
- 이 챕터의 응용 예제: 2D 그래픽에서 회전, 확대, 전단 변환을 적용한다.
- 어떤 정의가 계산을 가능하게 만들고, 어떤 정리가 계산을 정당화하는가?
- 반례를 만들려면 어떤 가정을 먼저 제거해 볼 수 있는가?

## 03. 행렬식과 역행렬

### 핵심 개념
- 벡터
- 행렬
- 선형변환
- 기저

### 이론 골격
- 벡터공간은 덧셈과 스칼라배가 닫혀 있는 추상적 좌표 공간이다.
- 기저는 모든 벡터를 유일하게 표현하게 하는 최소 생성 집합이다.
- 행렬은 선형변환의 좌표 표현이고, 행렬곱은 변환의 합성이다.
- 고유값과 고유벡터는 변환이 방향을 보존하면서 크기만 바꾸는 특별한 축을 나타낸다.

### 반드시 이해할 정리/원리
- 랭크-널리티 정리는 해공간의 자유도와 변환 이미지의 차원을 보존 관계로 묶는다.
- 스펙트럼 정리는 대칭행렬을 직교기저에서 대각화해 해석을 단순화한다.
- 최소제곱은 관측 벡터를 열공간에 정사영하는 문제로 해석된다.
- SVD는 임의 행렬을 회전, 축척, 회전의 조합으로 분해해 저차원 근사를 가능하게 한다.

### 공부할 때의 기준 질문
- 이 챕터의 기본 예제: 행렬식의 부호와 크기가 면적 배율을 뜻함을 확인한다.
- 이 챕터의 응용 예제: 지도 좌표 변환에서 면적 왜곡률을 계산한다.
- 어떤 정의가 계산을 가능하게 만들고, 어떤 정리가 계산을 정당화하는가?
- 반례를 만들려면 어떤 가정을 먼저 제거해 볼 수 있는가?

## 04. 고유값과 고유벡터의 첫 만남

### 핵심 개념
- 고유값
- 고유벡터
- 장기 거동
- Markov chain

### 이론 골격
- 벡터공간은 덧셈과 스칼라배가 닫혀 있는 추상적 좌표 공간이다.
- 기저는 모든 벡터를 유일하게 표현하게 하는 최소 생성 집합이다.
- 행렬은 선형변환의 좌표 표현이고, 행렬곱은 변환의 합성이다.
- 고유값과 고유벡터는 변환이 방향을 보존하면서 크기만 바꾸는 특별한 축을 나타낸다.

### 반드시 이해할 정리/원리
- 랭크-널리티 정리는 해공간의 자유도와 변환 이미지의 차원을 보존 관계로 묶는다.
- 스펙트럼 정리는 대칭행렬을 직교기저에서 대각화해 해석을 단순화한다.
- 최소제곱은 관측 벡터를 열공간에 정사영하는 문제로 해석된다.
- SVD는 임의 행렬을 회전, 축척, 회전의 조합으로 분해해 저차원 근사를 가능하게 한다.

### 공부할 때의 기준 질문
- 이 챕터의 기본 예제: 반복 적용해도 방향이 유지되는 벡터를 찾는다.
- 이 챕터의 응용 예제: 고객 이동 확률 행렬의 장기 시장점유율을 추정한다.
- 어떤 정의가 계산을 가능하게 만들고, 어떤 정리가 계산을 정당화하는가?
- 반례를 만들려면 어떤 가정을 먼저 제거해 볼 수 있는가?
